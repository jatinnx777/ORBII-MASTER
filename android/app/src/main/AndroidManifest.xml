<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
  <!-- Opt-in only: private, on-device contact hashing for reward-fraud
       prevention. Requested at runtime solely after explicit consent in
       Settings. Play Store treats this as sensitive and needs the Permissions
       Declaration Form — remove this line before submitting if the
       contact-matching feature is left disabled. -->
  <uses-permission android:name="android.permission.READ_CONTACTS"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <uses-permission android:name="android.permission.WAKE_LOCK"/>
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
  <!-- Background Voice SOS: a microphone foreground service listens for the
       user's secret phrase on-device while the app is backgrounded. Opt-in. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
  <!-- Live victim tracking during an ACTIVE SOS only. Android 14+ requires an
       explicit FGS type permission; expo-location declares the service with
       foregroundServiceType="location". Without this the helper's map freezes
       on a stale pin the moment the victim's screen locks.
       ACCESS_BACKGROUND_LOCATION is a Play "sensitive permission" and needs the
       Permissions Declaration Form — the justification is exactly this: sharing
       a victim's live location with responders during an in-progress emergency. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
  <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
  <!-- Re-arm background Voice SOS after a device reboot (opt-in feature). -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
  <!--
    Force-strip sensitive permissions that dependencies (expo-image-picker,
    etc.) merge in but ORBII never uses. We only pick photos from the
    gallery (no camera capture) and write to the app sandbox (no external
    storage). Removing these shrinks the Play Protect "sensitive data" warning.
  -->
  <uses-permission android:name="android.permission.CAMERA" tools:node="remove"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
  <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" tools:node="remove"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <queries>
    <package android:name="com.google.android.googlequicksearchbox"/>
    <intent>
      <action android:name="android.speech.RecognitionService"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="true" android:theme="@style/AppTheme" android:supportsRtl="true" android:enableOnBackInvokedCallback="false">
    <meta-data android:name="expo.modules.notifications.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.notifications.default_notification_color" android:resource="@color/notification_color"/>
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="false"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="portrait" android:showWhenLocked="true" android:turnScreenOn="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter android:autoVerify="false">
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="orbii"/>
      </intent-filter>
    </activity>
    <!--
      Background Voice SOS — on-device speech recognition (Vosk) inside a
      microphone foreground service. No audio ever leaves the phone. Opt-in
      from Settings → Voice SOS → Background protection.
    -->
    <service
      android:name=".voice.VoiceGuardService"
      android:exported="false"
      android:foregroundServiceType="microphone"/>
    <!-- Restarts background Voice SOS after a reboot (only if it was armed). -->
    <receiver
      android:name=".voice.BootReceiver"
      android:exported="true"
      android:enabled="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED"/>
        <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
      </intent-filter>
    </receiver>
  </application>
</manifest>