<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Light, not DayNight. Every ORBII screen is cream and there is no dark
       theme. Under DayNight a phone in dark mode gave the window a black
       background, which showed through the tab cross-fade as a black flash. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:windowBackground">@color/appBackground</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
  </style>
  <!-- No-logo splash: just the solid brand colour. The animated-icon slot points
       at a transparent drawable so Android 12+ shows nothing there (and does not
       fall back to the launcher icon). -->
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_blank</item>
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>
</resources>