node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
*.log
.DS_Store
.env.local
.env.*.local
ios/

# --- Android (RN native): track SOURCE, exclude build artifacts + secrets ---
/android/build/
/android/.gradle/
/android/.cxx/
/android/app/build/
/android/app/.cxx/
# Secrets — kept ONLY in the local tar.gz backup, never in git:
/android/keystore.properties
/android/app/orbii-release.keystore
/android/local.properties
# Flutter native dir (Flutter retired; orbii_flutter slated for removal)
/orbii_flutter/android/
/orbii_flutter/ios/

# Vosk model download scratch (models live in android assets)
.voskdl/

# Kotlin compiler daemon session artifacts
android/.kotlin/

# Firebase ADMIN service-account keys — NEVER commit (full project access)
*firebase-adminsdk*.json
*-service-account*.json
serviceAccountKey.json

# Nested standalone repos (own git histories, never commit into ORBII-MASTER)
/website/
/showcase/
