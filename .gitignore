node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
*.log
.DS_Store
.env.local
.env.*.local
ios/

# --- Android (RN native): track SOURCE, exclude build artifacts + secrets ---
/android/build/
/android/.gradle/
/android/.cxx/
/android/app/build/
/android/app/.cxx/
# Secrets — kept ONLY in the local tar.gz backup, never in git:
/android/keystore.properties
/android/app/orbii-release.keystore
/android/local.properties
# Flutter native dir (Flutter retired; orbii_flutter slated for removal)
/orbii_flutter/android/
/orbii_flutter/ios/
